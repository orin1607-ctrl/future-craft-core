// Owner Gate — no publish without explicit approval (Phase 4+)
function assertOwnerApproved_(contentQueueId) {
  throw new Error('Not implemented — skeleton only');
}

function canPublish_() {
  return false; // skeleton: always blocked
}
