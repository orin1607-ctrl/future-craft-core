import { Navigate } from 'react-router-dom';

/** שלב 2 — מפנה לטופס החי (VehicleForm) עם Accordion סעיפים 1–5 */
export default function DevVehicleNewFormFullPreview() {
  return <Navigate to="/dev/vehicle-form-live/full" replace />;
}
