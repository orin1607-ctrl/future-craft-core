# ביקורת Make — אחרי הפעלת בוט + תרחישים · Staging בלבד

**זמן:** 2026-07-21 ~13:55 UTC  
**מקור:** `public/project-001/make-three-scenarios-audit-result.json`  
**מגבלות:** אין שליחת WA · אין Production · אין מחיקת תור · אין תשובות להודעות ישנות

---

## ממצא מרכזי

ב-Make API כרגע **רק 2 תרחישים פעילים** — לא 3.  
**Whatsapp Bot (`5797671`) עדיין כבוי**, וזה התרחיש שאליו מצביע Gupshup (`…plyk4s` / Hook `2567320`).

לכן **עדיין לא מוכן** לבקש ממך «היי» לבדיקת זמן-אמת.

---

## 1. אילו תרחישים פעילים ומה התפקיד?

| # | תרחיש | ID | Hook | תפקיד |
|---|--------|-----|------|--------|
| 1 | **CO.CO Dalia DLR → Staging** | `9553017` | `4270688` (`…25kumdfy`) | DLR בלבד → Supabase Staging |
| 2 | **Integration Webhooks** | `5474839` | `2427152` (`…vd9j6vbw`) | בוט/AI + שליחת Gupshup `msg` |
| — | **Whatsapp Bot** | `5797671` | `2567320` (`…plyk4s`) | **כבוי** — זה ה-Hook של Gupshup החי |

תרחיש שלישי פעיל **לא נמצא** ב-API.

---

## 2. כפילות?

**אין כפילות Hook בין תרחישים פעילים** — כל פעיל על Hook אחר.  
אבל יש **פער חיבור**: Gupshup שולח ל-`…plyk4s` (Whatsapp Bot), בזמן שהבוט/AI הפעיל יושב על Hook אחר — ולכן **לא מעבד** את אותן הודעות Gupshup.

---

## 3. Whatsapp Bot פעיל ורץ בזמן אמת?

**לא.** `isActive=false`, `islinked=false`.  
הודעות נכנסות ל-Hook שלו **נערמות בתור** (כרגע 4) במקום לרוץ.

---

## 4. מודול 98 / toJSON?

- ב-**Whatsapp Bot** (כבוי): לא נבדקו כשלי ריצה חדשים; המודול ההיסטורי עם `{{toJSON(1)}}` עדיין הסיכון כשיופעל.
- ב-**CO.CO Dalia DLR** (פעיל): עדיין `{{toJSON(1)}}` במודול HTTP — **ישבר** כשיגיע אליו טראפיק.
- ב-**Integration Webhooks**: אין `toJSON` ב-mapper.

---

## 5. האם הודעות לקוח עוברות לבוט/AI?

**לא במסלול החי.**  
לקוח → Gupshup → Hook `2567320` (Whatsapp Bot) → **תור / כבוי**.  
**Integration Webhooks** (AI+Gupshup) פעיל אבל על Hook אחר — לא מקבל את ה-callback של Gupshup הנוכחי.

---

## 6. האם DLR במסלול נפרד ולא עוצר את הבוט?

**בתכנון Make:** כן — יש תרחיש DLR נפרד (`9553017`).  
**בפועל:** Gupshup עדיין דוחף ל-Whatsapp Bot; תרחיש DLR הייעודי כמעט לא מקבל טראפיק (`queue=0` על `…25kumdfy`).  
הפרדה אמיתית דורשת: Whatsapp Bot בלי forward שבור **או** הפניית Delivery ל-Hook של DLR.

---

## 7. מה בתור עכשיו? (HOLD — בלי תשובות ישנות)

Hook `2567320` — **4 פריטים** (לא עובדו, לא נשלחו תשובות):

| סוג | תוכן |
|-----|------|
| DLR `failed` | gs_id `2cbed084-…` |
| DLR `failed` | gs_id `fb3b8a1f-…` |
| inbound | «היי» מ-`972534338601` (ישן יחסית) |
| inbound | «היי» מ-`972534338601` (נוסף) |

**מדיניות:** לא לשחרר תשובות ישנות בלי אישור מפורש ממך.

---

## 8. לשלוח «היי» עכשיו?

**לא עדיין.**

### מה צריך לפני כן
1. **להפעיל** את תרחיש **Whatsapp Bot** (`5797671`) ב-Make (ON + linked)  
   **או** להעביר את כתובת ה-callback ב-Gupshup ל-Hook של **Integration Webhooks** (`…vd9j6vbw`) אם זה הבוט שאתה רוצה.  
2. לתקן `{{toJSON(1)}}` בתרחיש DLR (ולבטל/לתקן forward שבור ב-Whatsapp Bot אם נשאר).  
3. להחליט מה עושים עם 4 הפריטים בתור (מחיקת DLR / HOLD על inbound ישן).

אחרי שתאשר שהפעלת **Whatsapp Bot** (או MATCH URL חדש) — אבדוק שוב ואז אומר לך לשלוח «היי» מ-`0534338601`.
