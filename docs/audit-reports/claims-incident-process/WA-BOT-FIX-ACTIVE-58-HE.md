# תיקון: תרחיש WhatsApp Bot הופסק עקב שגיאה

**זמן:** 2026-07-21 ~16:47 UTC  
**תרחיש:** Whatsapp Bot `5797671` · Staging בלבד · **לא Production**  
**מקור:** `public/project-001/wa-bot-fix-active-58-result.json`

---

## תשובות

| # | שאלה | תשובה |
|---|------|--------|
| 1 | אותה שגיאת מודול 58 (HTTP 400)? | **כן** |
| 2 | התרחיש נעצר בגלל 58? | **כן** — לפני התיקון: `isActive=false`, `islinked=false`; לוגים של 58 עם `Error: 400 Bad Request` |
| 3 | תוקן ב-Staging? | **כן** (תיקון רך — ראו למטה) |
| 4 | נשאר Active אחרי הודעות? | **כן** — גם אחרי הודעה שלישית |
| 5 | E2E אחרי תיקון? | **כן** — היי / יוני / בדיקה · שלוש הצלחות |

---

## אבחון

- לפני: תרחיש **כבוי** (התראת Make תואמת).
- מודול **58** (Gupshup במסלול משני, אחרי AI 63): כשלים חוזרים `400 Bad Request`.
- מודול **87** (מסלול ראשי): הצליח גם קודם.
- `handleErrors` על 58 היה **true** → שגיאת HTTP כיבתה את התרחיש.

השוואת mapper (מקוצר):  
58 ו-87 זהים במבנה; ההבדל בתוכן ההודעה: `{{76.value}}` מול `{{86.value}}` (פלט Feeder אחרי כל AI).

---

## מה תוקן (Staging)

1. `parameters.handleErrors = false` על מודול **58**
2. Error handler: `builtin:Ignore` (id **98**) על 58
3. `maxErrors` בתרחיש: 19 → **50**
4. הפעלה מחדש: Active + linked

**לא נגענו:** AI Agent · Google Sheets · Production · לוגיקת שיחה

---

## E2E אחרי התיקון

| הודעה | סטטוס | משך Make |
|--------|--------|----------|
| היי | הצלחה | ~3994ms |
| יוני | הצלחה | ~3704ms |
| בדיקה | הצלחה | ~4120ms |

אחרי שלוש ההודעות: **isActive=true · islinked=true**.

---

## הערת המשך (אופציונלי)

התיקון מונע **כיבוי** התרחיש. אם עדיין רוצים שגם מסלול 58 *ישלח* בהצלחה (לא רק יתעלם מ-400) — צריך אישור נפרד לבדיקת תוכן `{{76.value}}` / מיפוי ההודעה.
