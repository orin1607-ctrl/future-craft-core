# דוח סיום — התראות, מסמכי רכב ודוחות

תאריך בדיקה: 14.08.2026  
סביבה: Oren Car Staging בלבד  
קישור שנבדק: https://orin1607-ctrl.github.io/future-craft-core/  
גרסת Pages שנבדקה בפועל: `deployed_ref=5727453 feat/incident-alerts-staging`  
Commit קוד מרכזי: `e433612`  
Commit תיקון העלאה בפועל: `a2112a9`  
Commit/גרסת Pages סופית שנבדקה: `5727453`

## 1. תלת־חצי → התראת קצין רכב: PASS

- לא היה תקין: ההתראה לא הייתה מובטחת במסך ההתראות מהמקור הקנוני של הביקורת; שמירה חוזרת השאירה תזכורות 30/7/1 ישנות פעילות.
- תוקן: מסך ההתראות קורא גם את רשומת `vehicle_inspections` כמקור אמת, מציג תאריך עתידי מעל 30 יום, ומשבית משפחת תזכורות קצין ישנה בשמירה חוזרת. החברה נלקחת מהרכב.
- נבדק בפועל: שמירת +3 חודשים ולאחריה +6 חודשים דרך טופס תלת־חצי, בדיקת DB, מעבר למסך ההתראות ובחירת מסנן קצין רכב.
- אחרי רענון וכניסה מחדש: ההתראה של +6 נשארה גלויה, ההתראות הישנות של +3 לא נשארו פעילות, והרכב/החברה היו נכונים.
- פתוח: אין.

## 2. סינון התראה חופשית: PASS

- לא היה תקין: המסנן הוסתר כאשר הספירה הייתה אפס, וקישור התראת נהג לא השתמש ב־`did:` הקיים.
- תוקן: מסנני קצין רכב והתראה חופשית תמיד גלויים; `?category=free` נתמך; התראת נהג מקשרת לכרטיס הנהג.
- נבדק בפועל: כרטיס רכב → הוסף התראה חופשית → כותרת ותאריך עתידי → שמירה → התראות → מסנן התראה חופשית.
- אחרי רענון וכניסה מחדש: ההתראה נשארה תחת המסנן ולא הופיע מידע מחברה B.
- פתוח: אין.

## 3. מסמכי רכב לאחר העלאה: PASS

- לא היה תקין: טופס Dalia תייג את כל ההעלאות כ־`vehicle_docs`; קישורי הרכב נשמרו רק בשמירה הסופית ולכן העלאה מוצלחת יכלה להיראות כחסרה.
- תוקן: רישיון, חובה, מקיף וצד ג׳ משתמשים בקטגוריות הקיימות והקנוניות; ברכב קיים עמודת הקישור מתעדכנת מיד לאחר העלאה וגם נשמרת שוב בשמירה הסופית. נוספה תצוגת צד ג׳ למיזוג המסמכים הקיים.
- נבדק בפועל במסלול האמיתי: כרטיס רכב → פרטי רכב → עריכה מלאה → סעיף 3 → העלאת PDF לרישיון, חובה ומקיף → שמירה. שלושת ה־URL נפתחו בהצלחה.
- אחרי רענון וכניסה מחדש: שלושת המסמכים הופיעו בכרטיס; לכל קטגוריה הייתה רשומת metadata אחת בלבד; לא נמצאה כפילות ולא נמצא מסמך חברה A בחברה B.
- פתוח: אין.

## 4. דוח ביקורות קצין רכב + קישורים: PASS

- לא היה תקין: חסרו סטטוס ופעולות לשורת ביקורת; לא היה deep-link לביקורת ספציפית; הייצוא כלל רק חמש עמודות.
- תוקן: הדוח מציג היסטוריה מלאה, סטטוס, `—` ללא מועד הבא, קישור לביקורת הספציפית וקישור לכרטיס הרכב. דף הביקורות פותח את פרטי הביקורת לפי `inspectionId`.
- נבדק בפועל: ארבע שורות היסטוריה, כולל ביקורת ישנה, שתי בדיקות תלת־חצי חדשות וביקורת ללא `next_due_date`; סינון מספר רכב/פנימי; פתיחת ביקורת; פתיחת רכב; בידוד חברה.
- אחרי רענון וכניסה מחדש: הדוח וההיסטוריה נשארו זמינים.
- ייצוא: 8 עמודות מיושרות — מספר רכב, פנימי, סוג, תאריך, מועד הבא, סטטוס ושני קישורים.
- פתוח: אין.

## 5. כל הדוחות והייצואים: PASS

נפתח ויוצא בפועל כל אחד מ־13 סוגי הדוחות:

טסטים; טיפולים; תאונות; ביקורות קצין רכב; ביטוחים לחידוש; סיכום רכבים; סיכום נהגים; הוצאות לפי תקופה; רווח והפסד; טיפולים מפורט; תאונות מפורט; הזמנות; סיכום לפי ספקים.

- כל פעולת ייצוא הופעלה מהכפתור הגלוי בממשק.
- כל Blob נשמר לקובץ CSV, נפתח ונקרא מחדש.
- נבדקו כותרות, בלוקים נכונים, יישור עמודות בדוח הקצין ו־CSV escaping.
- לא נמצאו שורות קצין כפולות או זליגת חברה B.
- פתוח: אין.

## Regression וסגירה

- PASS: רשימות רכבים ונהגים, כרטיסי רכב/נהג, תלת־חצי, התראות, מסמכים, דוחות, מעקב רכבים, הגדרות התראות/חברה וסוגי הרכב החדשים.
- מנגנון הסתרת דשבורד נהג נבדק כבוי/פעיל ובבידוד חברה.
- Restore Point `c667595` והקבצים תחת `restore-c667595` לא שונו ולא נדרסו.
- הבדיקות בוצעו רק בחברות `QA-C5710-A-*` ו־`QA-C5710-B-*`.
- ניקוי נתוני QA וקובצי Storage הסתיים בהצלחה.
- `productionTouched=false`: לא בוצעה גישה או פריסה ל־Production, Hostinger, Supabase Production, Google Apps Script, IntakeV18 או מערכת התזרים.

ראיות מכונה: `final-alerts-docs-reports-qa/report.json` (`pass=true`, `cleanup.ok=true`).
