# סגירת משימות 5 / 7 / 8 / 10 — Staging Pages

- תאריך: 2026-08-13
- אתר: https://orin1607-ctrl.github.io/future-craft-core/
- ענף: `feat/incident-alerts-staging`
- קוד פורסם: `16ccb24` (`STAGING-DEPLOY.txt`)
- Snapshot נשמר: `c667595`
- Production / Hostinger / Supabase Production: לא נגענו
- חברות QA בלבד: `QA-C5710-A-*` / `QA-C5710-B-*` — נוקו בסוף

## תוצאה

| משימה | סטטוס |
|---|---|
| 1 חיפוש 19 | Regression PASS |
| 2 / 3 / 6 / 9 | לא שונו · Regression PASS |
| 4 התראות תוקף | Regression PASS |
| **5 מסמכי רכב** | **PASS** |
| **7 הסתרת דשבורד נהג** | **PASS** |
| **8 דוח ביקורות קצין** | **PASS** |
| **10 סוגי רכב בטופס** | **PASS** |

דוח מכונה: `report.json`

## מה תוקן (מינימום על הקיים)

### 5 — מסמכים
חיבור העלאה הקיים (`/documents` + `uploadDocument` + `license_doc_url` / `insurance_doc_url`).  
תיקון התאמת מספר רכב בטעינה/שמירה, פתיחה לפי `?category=`, כפתורי העלאה מכרטיס הרכב.

בדיקת אמת: העלאת PDF רישיון + ביטוח בחברת QA → URL נשמר → הקובץ נפתח → רענון + כניסה מחדש לכרטיס → אין כפילות → חברה B לא רואה את מסמכי A.

### 7 — דשבורד נהג
אותו `hidden_buttons` / `driver-hub-dashboard`.  
נוסף: חסימת קישור ישיר `/dashboard?driverId=` למנהל צי כשהאפשרות כבויה, והסתרת picker לפי חברת היעד. מנהל-על לא שונה.

בדיקת אמת: רשימה + כרטיס נהג + URL ישיר מוסתרים כשכבוי → אחרי הפעלה הכפתור חוזר → חברה B לא מושפעת.

### 8 — דוח קצין רכב
לחיצה על כרטיס/סינון פותחת את **הטבלה** (`data-report-table`).  
עמודות: מספר רכב, פנימי, סוג, תאריך, מועד הבא. חסר מועד הבא = `—`.  
ייצוא קיים. בידוד בין חברות.

נראה בפועל בטבלה:
- `CA044015` · `19` · תלת/חצי שנתית · `10.1.2026` · `—`
- אותה שורה חדשה עם מועד הבא `13.11.2026`

### 10 — סוגי רכב
קריסה בטופס הוספה (`introVehicleType` לפני `useMemo`) תוקנה.  
כפתור «הוספת רכב» בכותרת + מסך פתיחה עם נגרר / טרקטור / ציוד הנדסי / רכב זעיר + סוגים ישנים, בלי כפילות.

## Regression (1 / 2 / 3 / 4 / 6 / 9)

לא שונה קוד של המשימות האלה. על Pages: חיפוש 19, כרטיס רכב, התראות תוקף, התראה חופשית, בידוד חברות — יציבים.
