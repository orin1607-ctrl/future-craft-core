# משימה 11 — דוח אמת QA (משימות 1–10 + ייצוא)

- תאריך: 2026-08-13
- אזור: Orin Staging בלבד (`feat/incident-alerts-staging`, DB `usfeoerkpcafxxlyuldl`)
- UI: Vite מקומי `http://127.0.0.1:8081` + `.env.local` → Staging (לא Pages, לא Production)
- Snapshot: `docs/audit-reports/oren-car-tasks-1-10-staging/restore-c667595` — לא נמחק / לא שונה
- Production / Hostinger / Supabase Production / GAS: לא נגענו
- נתוני בדיקה אחרונים: חברה `QA-T11-A-1786642064782`, רכב `T1164782` פנימי `19`, נהג `נהג QA T11 1786642064782`
- Evidence: `docs/audit-reports/oren-car-tasks-1-10-staging/qa-task11/` + `report.json`

## תיקונים מינימליים שבוצעו במסגרת משימה 11

1. ייצוא רכבים פעילים — נוספה עמודת **מספר פנימי** (+ מחלקה) ב-`Vehicles.tsx`.
2. העלאת מסמך רכב בהקשר רכב — `Documents.tsx` שולח לוחית מה-context ומעדכן `license_doc_url` לרישיון.
3. ייצוא דוחות — נוספו בלוקים **סיכום רכבים** ו**סיכום נהגים** ל-`exportCSV` ב-`Reports.tsx`.

לא בוצע שינוי schema נוסף. לא מוזג `dalia_form_config`.

---

## משימה 1 — חיפוש לפי מספר פנימי

| | |
|---|---|
| מה נבדק בפועל | חיפוש `"19"` ברשימת רכבים; סדר מדויק מול `019`/`119`/`190`; חיפוש יצרן/לוחית/מחלקה |
| מסלול | רשימת רכבים → שדה חיפוש |
| נתונים | ToyotaQA פנימי `19` (`T1164782`); Mazda `019`; Ford `119`; Kia `190` |
| תוצאה | **PASS** |
| באג | לא |
| תיקון | לא נדרש |
| בדיקה חוזרת | כן — במסלול UI |
| לא אומת | חיפוש על נתוני בארי אמיתיים (נבדק בחברת QA ייעודית) |

---

## משימה 2 — תלת־חצי + התראת קצין רכב

| | |
|---|---|
| מה נבדק בפועל | מסלול מלא: רשימה → כרטיס → בדיקת תלת־חצי; +3/+6; שמירה; `next_due_date`; `vehicles.next_inspection_date`; התראת קצין; 30/7/1 בלי ריצה |
| מסלול | `/vehicles?vehicleId=…&view=hub` → בדיקת תלת־חצי / `/private-vehicle-inspection` |
| נתונים | `T1164782`; inspection_date `2026-08-13`; next_due `2026-11-13` (+3) |
| תוצאה | **PASS** |
| באג | לא במוצר; ב-QA נוצרה בדיקה שנייה בגלל שמירת ק"מ נמוך (משימה 3) — לא כפילות מערכתית |
| תיקון | לא |
| בדיקה חוזרת | כן |
| לא אומת | לחיצה ידנית על +6 כשמירה סופית נפרדת (נבדקו שני הכפתורים; נשמר +3) |

---

## משימה 3 — קילומטראז'

| | |
|---|---|
| מה נבדק בפועל | הזנת `85000` בבדיקה; דשבורד מציג `85,000`; ניסיון `1000` לא דורס |
| מסלול | טופס תלת־חצי → שמירה → דשבורד רכב → שמירה שנייה עם 1000 |
| נתונים | 85000 → נשמר; אחרי 1000 עדיין 85000 |
| תוצאה | **PASS** |
| באג | לא |
| תיקון | לא |
| בדיקה חוזרת | כן |
| לא אומת | הזנת פסיקים ידנית בשדה (`85,000` כטקסט) — נבדק תצוגה עם פסיקים אחרי מספר |

---

## משימה 4 — התראות בכרטיס רכב / נהג

| | |
|---|---|
| מה נבדק בפועל | יומן `/alerts/log` (לא Mock); פעילות / עתידיות (>30) / היסטוריה (עבר + לא פעילה); הסר מהפעילות; התראות מרכזיות |
| מסלול | כרטיס רכב → התראות ושליחות; כרטיס נהג → יומן; `/alerts` |
| נתונים | התראה קרובה (חופשית); עתידית +80 יום; עבר מועד; `is_active=false`; התראת קצין ממשימה 2 |
| תוצאה | **PASS** |
| באג | סקריפט QA פנה קודם ל-`/notification-log` (404) — זה לא באג מוצר. הנתיב האמיתי: `/alerts/log` |
| תיקון | תיקון סקריפט QA בלבד |
| בדיקה חוזרת | כן אחרי תיקון הנתיב |
| לא אומת | מחיקת היסטוריה מה-DB (אסור ואכן לא בוצע) |

---

## משימה 5 — מסמכי רכב (רישיון)

| | |
|---|---|
| מה נבדק בפועל | העלאת PNG רישיון בהקשר רכב; metadata עם לוחית נכונה; הופעה בכרטיס → פעולות → מסמכים; אין כפילות ברורה |
| מסלול | `/documents?vehicleId=…&plate=…` → רישיונות רכב → העלאה → חזרה ל-hub מסמכים |
| נתונים | `qa-license.png` על `T1164782` |
| תוצאה | **PASS** (אחרי תיקון מינימלי) |
| באג | העלאה בהקשר רכב לא העבירה לוחית → metadata בלי plate → לא הופיע ב-hub |
| תיקון | `Documents.tsx`: `plateForUpload = uploadVehicle \|\| contextPlate`; עדכון `license_doc_url` לרישיון |
| בדיקה חוזרת | כן — PASS |
| לא אומת | דיאלוג Preview ייעודי מעבר להופעה ב-hub; מסמכי בארי קיימים (לא נפגעו כי נעשה בחברת QA נפרדת) |

---

## משימה 6 — התראה חופשית + סוג קצין רכב

| | |
|---|---|
| מה נבדק בפועל | רכב: פעולות רכב → התראה → מודל (סוג חופשית + קצין); נהג: כפתור התראה חופשית; שמירה; שיוך `vplate`/`did`; יומן + התראות מרכזיות; אין ערבוב |
| מסלול | כרטיס רכב → פעולות רכב → התראה; כרטיס נהג → התראה חופשית |
| נתונים | `QA חופשית רכב קרובה · T1164782`; `QA חופשית נהג · נהג QA T11 …` |
| תוצאה | **PASS** |
| באג | לא במוצר |
| תיקון | לא |
| בדיקה חוזרת | כן |
| לא אומת | התראת קצין ידנית ממודל הנהג (הסוג נראה; נוצרה התראת קצין אוטומטית ממשימה 2) |

---

## משימה 7 — מתג „פתח דשבורד נהג”

| | |
|---|---|
| מה נבדק בפועל | מנהל-על → הגדרות חברות → הסתרת `פתח דשבורד נהג`; FM של חברה A לא רואה; SA כן; חברה B לא הושפעה; דשבורד רכב לא הושפע |
| מסלול | `/alert-settings` → בחירת חברה A → שמירה; כרטיס נהג A/B; כרטיס רכב B |
| נתונים | A: `QA-T11-A-…` (הוסתר); B: `QA-T11-B-…` (ללא שינוי) |
| תוצאה | **PASS** |
| באג | לא |
| תיקון | לא |
| בדיקה חוזרת | כן |
| לא אומת | מתג על חברת בארי/דליה חיה — נבדק בחברות QA בלבד (לפי גבולות Staging) |

---

## משימה 8 — דוח ביקורות קצין רכב

| | |
|---|---|
| מה נבדק בפועל | דוחות → סינון תקופה „הכל” → כרטיס ביקורות קצין; מספר רכב, פנימי 19, סוג, תאריך, מועד הבא; סינון לוחית/פנימי; בדיקה חדשה ממשימה 2; ביקורת ישנה בלי `next_due_date` |
| מסלול | `/reports` |
| נתונים | חדשה: תלת/חצי `13.8.2026` → הבאה `13.11.2026`; ישנה רבעונית `15.1.2025` מועד הבא `-` |
| תוצאה | **PASS** |
| באג | לא במוצר |
| תיקון | לא |
| בדיקה חוזרת | כן |
| לא אומת | סינון שמיישם בפועל צמצום טבלה עד שורה אחת (נבדק פתיחת פילטר + הופעת האפשרות `T1164782` / `19`) |

---

## משימה 9 — הערות רכב ונהג

| | |
|---|---|
| מה נבדק בפועל | הערה ארוכה + „הצג ברשימה” כבוי/פעיל לרכב ולנהג; נשמר ב-`notes` / `drivers.notes`; מעקב רכבים לא נשבר |
| מסלול | כרטיס רכב → ניהול רכב → הערה; רשימת רכבים; כרטיס נהג → היסטוריה והערות; רשימת נהגים; `/vehicle-tracking` |
| נתונים | `הערת רכב ארוכה…`; `הערת נהג ארוכה…` |
| תוצאה | **PASS** |
| באג | לא |
| תיקון | לא |
| בדיקה חוזרת | כן |
| לא אומת | לוגיקות ישנות אחרות שקוראות `notes` מחוץ לרשימה/כרטיס/מעקב |

---

## משימה 10 — סוגי רכב + dalia_form_config

| | |
|---|---|
| מה נבדק בפועל | הגדרות סוגי רכב + טופס רכב חדש (שלב מלא): נגרר / טרקטור / ציוד הנדסי / רכב זעיר; בחירה; שמירה; פתיחה מחדש; סוגים ישנים; בלי כפילות |
| מסלול | `/admin/modules/vehicles/vehicle-types`; רכבים → רכב חדש → המשך לטופס המלא |
| נתונים | 4 רכבי TypeQA עם הסוגים החדשים + ToyotaQA `רכב פרטי` |
| תוצאה | **PASS** |

### dalia_form_config — דיווח בלבד (לא מוזג)

- בשורת העבודה **אין** רשומת `dalia_form_config.vehicle_types`.
- לכן **כל החברות** באזור העבודה מקבלות אוטומטית את `DEFAULT_VEHICLE_TYPES` כולל ארבעת הסוגים החדשים.
- `companiesMissingNewTypes`: **ריק**.
- לא שונה / לא מוזג קונפיג.

חברות קיימות ב-`company_settings` (רשימה מלאה ב-`report.json`): דליה, קיבוץ בארי, ק'יבוץ בארי, אילנה אטיאס, דרכי חיים, וכו' + חברות QA/E2E. כולן תחת ברירת המחדל כל עוד אין קונפיג מותאם.

| לא אומת | שמירת טופס Dalia מלא מקצה לקצה לכל 4 הסוגים דרך כל שדות החובה (נבדקה בחירה ב-select + persist ב-DB + הופעה אחרי reopen) |

---

## טבלת ייצוא

| ייצוא | מסלול | קובץ שנפתח | כותרות / מספר פנימי | רשומות מול מערכת | תוצאה |
|---|---|---|---|---|---|
| כל הרכבים הפעילים (Excel/CSV) | רשימת רכבים → פעיל → ייצוא | `exports/vehicles_2026-08-13.csv` | יש עמודת **מספר פנימי**; `T1164782` ↔ `19`; `019`/`119`/`190` מיושרים נכון; ק"מ 85000; מחלקה QA-Dept | 8 רכבים פעילים בחברת QA (4 מקוריים + 4 סוגי רכב) | **PASS** |
| סיכום נהגים (רשימה) | רשימת נהגים → ייצוא | `exports/drivers_2026-08-13.csv` | שם מלא / טלפון / הערות; הנהג הנכון מופיע | 1 נהג בחברת QA | **PASS** |
| ביטוחים לחידוש | דוחות → ייצוא CSV | `exports/דוח_דליה_13.8.2026.csv` בלוק `--- ביטוחים לחידוש ---` + כותרות כולל `מס' פנימי` | בלוק קיים; שורות ריקות כי לרכבי QA אין תוקף ביטוח | **PASS** (מבנה+כותרות; אין נתוני ביטוח בסט QA) |
| סיכום רכבים (דוחות) | דוחות → ייצוא | אותו קובץ, בלוק `--- סיכום רכבים ---` | `19,T1164782,…`; גם 019/119/190/21–24 | 8 שורות = הרכבים הפעילים | **PASS** (תוקן במשימה 11 — הבלוק חסר קודם ב-exportCSV) |
| סיכום נהגים (דוחות) | דוחות → ייצוא | אותו קובץ, בלוק `--- סיכום נהגים ---` | הנהג הנכון; חברה נכונה | 1 שורה | **PASS** (תוקן במשימה 11) |
| ביקורות קצין בתוך ייצוא הדוחות | אותו קובץ | ישנה רבעונית בלי next_due = `-`; חדשה תלת/חצי + `13.11.2026`; פנימי 19 | **PASS** |

---

## Regression ממוקד

| מסלול | תוצאה |
|---|---|
| רשימת רכבים | PASS |
| רשימת נהגים | PASS |
| כרטיס רכב | PASS |
| כרטיס נהג חדש | PASS |
| מסמכי רכב | PASS |
| התראות מרכזיות | PASS |
| הגדרות חברות | PASS |
| דוחות | PASS |
| מעקב רכבים | PASS |

---

## סיכום

**האם משימות 1–10 מוכנות לבדיקה שלכם באזור העבודה? כן.**

- כל סעיפי 1–10 נבדקו במסלול UI אמיתי (לא רק קוד/Unit Test).
- ייצוא הרכבים הפעילים כולל מספר פנימי מיושר לרכב הנכון.
- Snapshot נשמר.
- אין אישור ל-Production ואין אישור ל-Hostinger.
- הקוד של 1–10 + תיקוני 11 עדיין **לא committed / לא על GitHub Pages** — הבדיקה שלכם צריכה Vite מקומי או Deploy ל-Staging לפי החלטתכם.
