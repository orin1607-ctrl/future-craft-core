# 05 — מסמך לעורך דין (עובדתי — אינו חוות דעת משפטית)
**תאריך:** 2026-07-22  
**מטרה:** לספק עובדות טכניות להכרעות משפטיות בנושא פרטיות, אבטחת מידע, ספקים והתקשרויות.

## 1. תיאור השירות
דליה מספקת מערכת SaaS לניהול, תפעול ותחזוקת ציי רכב: רכבים, נהגים, תקלות, תאונות, הזמנות שירות, מסמכים, התראות, WhatsApp/Email, מנויים.

## 2. משתמשים
- מנהלי צי (`fleet_manager`)
- נהגים (`driver`)
- לקוחות פרטיים (`private_customer`)
- מנהלי מערכת דליה (`super_admin`)

## 3. מידע שנשמר (עובדתי טכני)
ראה מטריצה ב-`03-DATA-AND-PRIVACY-MAP.md`. כולל זהויות, רכבים, תקלות/תאונות, הצהרות, מסמכים, לוגי משלוחים, נתוני מנוי.

## 4. תשתית ואזור
- פרונט: Hostinger VPS, דומיין `dalia-car.online` (IP מאומת).
- Backend: Supabase project `qasomfndnjuixgjmjwcm`; אזור מתועד בתיעוד כ-`ap-south-1` (**לא אומת מ-Dashboard בסשן זה**).
- הודעות: Gupshup (מאומת Prod), Resend (מאומת ב-E2E), Make (מתועד).

## 5. ספקי משנה
ראה `04-SUBPROCESSORS-AND-VENDORS.md`. **לא נמצאו** במסגרת הבדיקה קבצי DPA/הסכמים חתומים.

## 6. הרשאות והפרדה
- RBAC עם 4 תפקידים.
- הפרדת חברות מבוססת `company_name` (טקסט) + RLS בקוד/מיגרציות.
- **לא בוצע dump חי של כל ה-Policies מ-Prod** (חסר DB/Management access).

## 7. WhatsApp
- שליחה עסקית דרך Gupshup Edge Functions — **מאומתת היום ב-Production**.
- נשמר ב-`incident_notification_deliveries`.
- Webhook DLR: `gupshup-webhook` חי.
- בנוסף deep-links מסוג wa.me למקרי חירום/שיתוף.

## 8. שמירה / מחיקה / סיום התקשרות
**לא נמצא** נוהל מחיקת לקוח מקצה לקצה או TTL אוטומטי מאומת.  
קיימים כלי ייצוא/גיבוי בקוד (`backup-data`, `full-supabase-export`) — השפעה על זכויות נושא מידע דורשת הכרעת עו״ד.

## 9. אירועי אבטחה
לא נמצא נוהל אירוע אבטחה כתוב ומאומת במערכת. יש `system_logs` / `system_update_audit` חלקיים.

## 10. מסמכים קיימים שנמצאו ב-repo
תיעוד טכני רב תחת `docs/` (Gupshup/Make/Environment).  
**לא נמצאו** במסגרת זו: מדיניות פרטיות ללקוח, תנאי שימוש חתומים, DPA, SLA משפטי.

## 11. שאלות פתוחות לעו״ד (לא מוכרעות כאן)
1. האם דליה בעלת שליטה / מעבדת / משותפת — לפי מודול?
2. האם חלה חובת רישום/הודעת מאגר?
3. רמת אבטחת מאגר הנדרשת?
4. העברות מחוץ לישראל (Supabase region, Gupshup, Resend, PayPal, AI) — בסיס חוקי?
5. האם הצהרות נהג/תאונות = מידע רגיש מיוחד?
6. שקיפות לנהגים מול מעסיק?
7. חובות דיווח באירוע אבטחה?
8. ניסוחי שיווק מותרים?

## 12. נספח טכני רגיש
הפניה ל-`02-VERIFIED-FINDINGS-REGISTER.md` — **ללא** פירוט ניצול מעשי מעבר לנדרש להבנה.
